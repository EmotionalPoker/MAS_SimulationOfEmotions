from tkinter import *
from tkinter import  messagebox
import re
import os

window=Tk()
window.title("page3")
window.geometry("900x600" )
heading = Label(text = "Welcome to Game").pack()
Label(window,text="Community Cards").place(x=15,y=50)


window.mainloop()
